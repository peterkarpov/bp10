@model ESN3.WebUI.Models.ProfileViewModel
@{
    ViewBag.Title = "Create";

    
}

<div class="section">
    <div class="row">
        <div class="col-md-10">
            <div class="col-md-offset-2 col-md-8">

                <h2>Create Profile</h2>

                @using (Html.BeginForm())
                {
                    @Html.ValidationSummary()

                    @*<div class="form-group">
                        <label>First Name</label>
                        @Html.TextBoxFor(m => m.Profile.fName, new { @class = "form-control text-center" })
                    </div>

                    <div class="form-group">
                        <label>@Html.LabelFor(m => m.Profile.Gender)</label>
                        @Html.EnumDropDownListFor(m => m.Profile.Gender, new { @class = "form-control text-center" })
                    </div>

                    <div class="form-group">
                        <label>@Html.LabelFor(m => m.Profile.dob)</label>
                        @Html.EditorFor(m => m.Profile.dob, new { @class = "form-control text-center" })


                    </div>*@

                        @*<div class="form-group">
                                <div style="position:relative;">
                                    <label>Картинка</label>
                                    <a class='btn' href='javascript:;'>
                                        Выберите файл...
                                        <input type="file" name="Image" size="40"
                                               style="position:absolute;z-index:2;top:0;
                                    left:0;filter: alpha(opacity=0); opacity:0;
                                    background-color:transparent;color:transparent;"
                                               onchange='$("#upload-file-info").html($(this).val());'>
                                    </a>
                                    <span class='label label-info' id="upload-file-info"></span>
                                </div>
                                @if (Model.Profile.ImageData == null)
                                {
                                    <div class="form-control-static">Нет картинки</div>
                                }
                                else
                                {
                                    <img class="img-thumbnail" width="150" height="150"
                                         src="@Url.Action("GetImage", "Image",
                                                           new { Model.Profile })" />
                                }
                            </div>*@

                        <button type="submit" class="btn btn-default">
                            Create
                            <br>
                        </button>
                }
            </div>
        </div>
    </div>
    @*</div>*@
</div>



